#ifndef GRAPHGENERATION_HPP
#define GRAPHGENERATION_HPP

#include "common.hpp"
#include "userInput.hpp"
#include "graphicAlgorithms.hpp"
#include "treeAlgorithms.hpp"


void genTestNodes (SharedData* sharedData);


void initGraph (SharedData *sharedData);


void deletegraph (SharedData *sharedData);


void newGraph (SharedData *sharedData);


void clearGraph (SharedData *sharedData);

#endif
